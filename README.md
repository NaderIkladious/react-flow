# react-flow

TypeScript + React library scaffold.

## Scripts
- `npm run build` - compile TypeScript to `dist/`
- `npm run clean` - remove and recreate `dist/`

## Structure
- `src/` source for the library
- `dist/` build output (placeholder `.gitkeep` so the folder exists)
- `example/` sample usage entry point
